services:
  - type: web
    name: thinki-question-generator
    env: python
    pythonVersion: 3.12.10
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GEMINI_API_KEY
        sync: false